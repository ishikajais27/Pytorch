Tensor Properties

What it is: Attributes describing the tensor.

Inputs: Tensor
Outputs: Information about tensor

Properties:

shape → size of tensor, e.g., [2,2]

dtype → data type, e.g., float32

device → CPU or GPU

requires_grad → tracks gradients for backpropagation
